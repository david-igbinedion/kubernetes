name: "Terraform action"
on:
  push:
    branches:
      - main
 
env:
  TF_LOG: INFO
  AWS_REGION: us-east-2
jobs:
  deploy-serverless:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: serverless
    steps:
      - name: Git checkout
        uses: actions/checkout@v3

      - name: Checkout code repository
        uses: actions/checkout@v3
        with:
          repository:  getir-dev/david-igbinedion-serverless-application
          token: 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 #should be stored as a secret but I don't have admin access to do so

      - name: List directory
        run: ls

      - name: Zip code
        uses: montudor/action-zip@v1
        with:
          args: zip -qq -r david-igbinedion-serverless-application.zip david-igbinedion-serverless-application

      - name: List directory
        run: ls 
